# Checkpoint - after Lesson 07 (The Command Line & Git)

Reference "on-track" state. A student who fell behind can switch here to rejoin
from a known-good baseline; instructors use it as the worked answer key. Lesson
08 (Sprint 1) points catch-up students at this branch.

What should be true now:

- You worked the team repo entirely from the terminal: `status`, a branch, an
  edit made with `echo >>`, `add`, `commit`, `push`.
- The "Setup log" line in `docs/README.md` is the reference artifact from that
  command-line rep.

Everything else matches `main`.

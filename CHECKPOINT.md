# Checkpoint - after Lesson 05 (Git I)

Reference "on-track" state. A student who fell behind can switch here to rejoin
from a known-good baseline; instructors use it as the worked answer key.

What should be true now:

- You created a branch and committed to it.
- You added yourself to the Team section of `docs/README.md` (see the sample
  entry) and opened/merged that as a commit.

Everything else matches `main`.

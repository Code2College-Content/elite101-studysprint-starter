# Checkpoint - after Lesson 13 (Sprint 2 + Bug Bash)

Reference "on-track" state. A student who fell behind can switch here to rejoin
from a known-good baseline; instructors use it as the worked answer key.

What should be true now:

- All four seeded bug-bash bugs are fixed: the swapped score line in `main.py`,
  the read-mode save in `storage.py`, the case-sensitive `check_answer` in
  `features/core.py`, and the wrong key in `deck_summary` in
  `features/reports.py`.
- Every file matches `main` - `main` is the correct reference.

`python -m unittest` passes.
